# Veva Jewels

This is a full-stack project for Veva Jewels, including:

- **Frontend**: React
- **Backend**: Spring Boot + MongoDB

## Folders

- `frontend/` – React app (UI)
- `backend/` – Spring Boot (APIs)
